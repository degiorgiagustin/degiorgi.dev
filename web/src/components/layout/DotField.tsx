// Signature dot-grid background layer (spec 001 §3).
// Server component — static paint, no interactivity. Decorative, so aria-hidden.
export function DotField() {
  return <div aria-hidden className="dot-field" />;
}
